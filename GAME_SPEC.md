# ピンポン ゲーム仕様

## 1. 概要

スマートフォンまたはPCのブラウザで遊べる、縦画面の2Dピンポンゲーム。
スマートフォン1台を挟んだ2人対戦を基本モードとし、1人対CPUにも切り替えられる。
通常ルールと、5種類の10秒アイテムが登場するパーティールールを選択できる。

- 内部ゲーム座標: 480 x 800
- 勝利条件: 5点先取
- 実装: HTML / CSS / JavaScript / Canvas API / Pointer Events
- 配布形態: インストール可能なPWA

## 2. プレーモード

### 2人対戦

- 初期選択されるメインモード
- 1Pは画面下側、2Pは画面上側を担当する
- 2本の指を同時に追跡し、上下のラケットを独立して操作できる
- 上側のスコアとカウント表示は、端末の反対側から読める向きで表示する
- 2Pが勝った場合は結果画面を2P側へ向ける
- 一時停止画面には1P側と2P側の両方へ操作ボタンを表示する
- CPU難易度は使用しない

### 1人 vs CPU

- プレイヤーは画面下側、CPUは画面上側を担当する
- CPU難易度は弱・中・強から選択する
- 強ではレベル1から5を追加選択できる

### 対戦ルール

- 通常: アイテムなしの基本対戦
- パーティー: 対戦中に5種類のアイテムが登場する
- 対戦相手と対戦ルールは別々に選択でき、パーティーは2人対戦とCPU対戦の両方で使用できる

## 3. 基本ルール

- 両陣地の奥中央にゴールを1つずつ置く
- 相手側のゴールへボールを入れると1点
- ゴール以外の外壁と左右の壁ではボールが反射する
- ラケットへ当たったボールは、接触面の法線とラケット速度を使って反射する
- サーブは205の低速から始まり、ラケットへ有効に当たるたび速度が28ずつ増える
- 壁への反射では加速せず、最高速度は660とする
- 高速時もすり抜けないよう、1回の物理移動を最大6座標単位へ分割する
- 得点後は短く停止し、3秒カウント後に次のラリーを始める
- サーブ方向は得点ごとに交互に切り替える
- 先に5点取った側が勝者となる

## 4. 2人用タッチ操作

- Pointer Eventsを使用し、`pointerId`ごとに入力を管理する
- タッチ開始位置が中央線より上なら2P、下なら1Pへ割り当てる
- 一度割り当てた指は、中央線をまたいでも担当を変えない
- 各陣地で有効なポインターは1本までとする
- 上側と下側のポインターは同時に操作できる
- 上側ラケットは指より中央側の下へ、下側ラケットは指より中央側の上へずらして表示する
- ラケットは自陣と壁の内側だけを移動できる
- PCではマウス位置がある側のラケットを操作する
- フリーズ弾の効果中は、移動量10px以内かつ240ms以内の短いタップで発射する
- 移動用の指を置いたまま、同じ陣地を別の指で短くタップして発射できる

## 5. オートバランス

点差が開いたとき、負けている側のラケットだけを拡大する。
リード側のラケットは標準サイズを維持する。

| 点差 | 負け側のサイズ |
| --- | --- |
| 0から1点 | 100% |
| 2点 | 115% |
| 3点以上 | 130% |

- サイズ変更は得点直後にアニメーションする
- 補正は次のラリーへ持ち越す
- 描画、当たり判定、移動可能範囲、タッチ補正へ同じサイズを適用する
- スコア付近に現在の補正率を表示する

## 6. ラケット形状

試合前に次の3形状から選択する。選択は両者共通とする。

- オーバル: 標準的な楕円形
- ワイド: 横方向に広く、縦方向に薄い四角形
- スター: 外側と内側の頂点を交互に持つ星形

各形状は同じ頂点データを次の処理で共有する。

- Canvas描画
- ボールとの当たり判定
- 壁および中央線に対する移動制限
- オートバランスによる拡大

ボールとの接触は円対ポリゴンとして判定する。最近辺、点の内外、重なり量を求め、見た目の輪郭に沿ってボールを押し出して反射させる。

## 7. パーティールール

### アイテムの出現

- ラリー開始から3.5秒後に最初のアイテムを中央から発射する
- 取得または退場後、5.5秒から8秒のランダムな間隔で次のアイテムを発射する
- コート上へ出る未取得アイテムは同時に1個までとする
- アイテムの半径はボールと同じ10、速度は発射時点の最速ボールと同じにする
- 発射角度はランダムとし、左右または角丸の側壁では速度を保って反射する
- 上下のゴール面では得点せず、そのまま通過して消える
- ラケットの見た目と同じポリゴンへ接触した時点で消え、接触した側が取得する
- ボール、アイテム、弾同士は衝突しない

### 効果時間

- 全効果はラリーが`PLAYING`の間だけ10秒を消費する
- カウントダウン、得点停止、一時停止中は残り時間を消費しない
- 効果は得点後の次ラリーへ持ち越す
- 同じ効果を再取得した場合は重複せず、残り時間を10秒へ戻す
- 試合開始、再戦、設定画面へ戻ったときは全効果を解除する

### 5種類の効果

1. 3ボール
   - ボールを合計3球に増やす全体効果
   - 最初にゴールを通過した1球だけで1点とし、その時点で全球を回収する
   - 逆側のゴールへ同時に入った場合はLETとして無得点で再サーブする
   - 効果が残っていれば次ラリーも3球で始める
2. ラケット拡大
   - 取得側のラケットを130%にする
   - オートバランスと乗算し、合計は155%を上限とする
3. ゴール縮小
   - 取得側が守る自分のゴール幅を標準の58%にする
   - 描画と得点判定へ同じ幅を使用する
4. 丸いコート
   - ゴールの平面を残し、コート四隅を最大半径112の角丸形状へ変える全体効果
   - ボール、アイテム、弾、ラケット制約へ同じ角丸形状を使用する
5. フリーズ弾
   - 取得側は相手方向へ垂直な弾を発射できる
   - 弾速は発射時点の最速ボールと同じ、連射間隔は1秒とする
   - 相手ラケットへ当たると弾は消え、相手は0.5秒移動できない
   - 弾が外壁またはゴール面へ出た場合は消える
   - CPUは射線が合ったとき、同じクールダウンで自動発射する

取得中は上下のスコア横に固定5スロットを表示し、効果の種類、残り秒数、射撃クールダウンを示す。取得時は取得側の向きに短い通知を表示する。

## 8. CPU

CPUは上側陣地内でボールを追跡する。
難易度により、反応間隔、最高移動速度、狙いの誤差を変更する。

- 弱: 反応が遅く、移動速度が低く、誤差が大きい
- 中: 標準的な反応と速度
- 強: 反応と速度が高く、レベル1から5で段階的に強化される

CPUモードの難易度は、卓上の配色とBGMにも反映する。
2人対戦では標準の卓球台テーマを使用する。

複数ボール時は上側ゴールへ最短時間で向かう球を優先して追跡する。
停止効果中は速度を0にし、解除後に通常追跡へ戻る。

## 9. ゲーム状態

JavaScriptでは次の状態を管理する。

- `MENU`: 対戦相手、通常・パーティールール、CPU難易度、ラケット形状を選択
- `COUNTDOWN`: ラリー開始前の3秒カウント
- `PLAYING`: 対戦中
- `SCORED`: 得点直後の停止
- `GAME_OVER`: 勝敗結果を表示
- `PAUSED`: 対戦を一時停止

結果画面では、同じ設定での再戦と設定画面へ戻る操作を提供する。

## 10. 表示とレスポンシブ対応

- ゲーム台は3:5の縦横比を維持する
- Canvasは表示サイズと`devicePixelRatio`へ追従する
- スマートフォンではページスクロール、選択、ピンチズームによる誤操作を防ぐ
- 設定パネルは低い画面でもゲーム枠内へ収め、必要な場合だけ内部スクロールする
- スコア、ボタン、ラベルは390 x 844相当の画面で枠外にはみ出さない
- 1Pと2Pは異なるリム色で識別する
- オートバランス中は補正率をスコア付近へ表示する
- パーティー中だけ効果HUDを表示し、通常ルールではゲーム画面の広さを維持する
- 上側の効果HUDと取得通知は、2人対戦時だけ180度回転する
- アイテムは色だけでなく固有記号、外周ハロー、軌跡でも区別する

## 11. 音、振動、PWA

- ラケット、壁、ゴール、勝敗で効果音を鳴らす
- 対戦中は難易度に応じたBGMを再生する
- 対応端末では得点時と決着時に振動する
- 音声や振動が利用できない場合もゲームを継続する
- Service Workerでアプリシェルをキャッシュし、オフライン起動へ対応する
- キャッシュ構成を変更した場合はキャッシュ名を更新する

## 12. WebRTC接続実験

接続実験はゲーム本体とは独立した検証機能として残す。

- Offer / Answerをコピーして2台をP2P接続できる
- orderedなcontrol DataChannelと、非ordered・再送なしのrealtime DataChannelを確認できる
- ping / pongでRTTを表示する
- 現時点ではゲーム入力やゲーム状態を同期しない

## 13. ファイル構成

```text
javascript-game/
├── index.html
├── style.css
├── game.js
├── connection-experiment.mjs
├── manifest.webmanifest
├── service-worker.js
├── icons/
├── tests/
└── GAME_SPEC.md
```

## 14. 完了条件

- スマートフォン1台で1Pと2Pを2本の指で同時操作できる
- 指が中央線をまたいでも担当ラケットが入れ替わらない
- 1人対CPUへ切り替えて操作できる
- 点差0から1では同サイズ、2点差では負け側115%、3点差以上では負け側130%になる
- サイズ補正が次のラリー中も描画と当たり判定へ反映される
- オーバル、ワイド、スターを選択でき、見た目と当たり判定が一致する
- 通常ルールでボールが低速から始まり、ラケットへ当たるたび一定量ずつ最高速度まで加速する
- パーティールールで5種類のアイテムが取得でき、各効果が実働10秒で終了する
- 3ボール中も1ラリーで最大1点となり、効果中の次ラリーでも3球へ戻る
- ラケット拡大とオートバランスが上限付きで合成される
- ゴール縮小と丸いコートで描画、衝突、得点判定が一致する
- 操作用の指を離さず別の指で発射でき、弾の1秒間隔と0.5秒停止が機能する
- 5点先取、得点、カウント、再戦、設定変更、一時停止が動作する
- 390 x 844相当の縦画面で設定とスコアがゲーム枠内へ収まる
- PWAとWebRTC接続実験の既存機能が回帰しない
